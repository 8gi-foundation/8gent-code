# use-click-outside

React hook to detect clicks outside a referenced element.

## Requirements
- useClickOutside(ref, handler) attaches event listener
- Handles touch events too
- Ignores portal children
- Cleans up on unmount
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/use-click-outside.ts`
