# monotonic-queue

Monotonic deque for sliding window min/max in O(1).

## Requirements
- MonotonicQueue<T>(compare) for min or max variant
- push(value) maintains monotonic order
- front() returns current optimum
- pop() removes oldest element
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/monotonic-queue.ts`
