# use-event-listener

React hook for adding and auto-cleaning event listeners.

## Requirements
- useEventListener(target, event, handler, options?) attaches listener
- Works on window, document, or any EventTarget
- Handles SSR
- Cleans up on unmount
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/use-event-listener.ts`
