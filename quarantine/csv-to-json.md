# csv-to-json

Convert CSV text to array of objects with header row support.

## Requirements
- parse(csv) returns array of row objects
- Headers from first row by default
- Handles quoted fields with commas
- trimFields option
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/csv-to-json.ts`
