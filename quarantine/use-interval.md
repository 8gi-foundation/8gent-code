# use-interval

React hook for declarative setInterval with cleanup.

## Requirements
- useInterval(callback, delay) sets up interval
- delay=null pauses the interval
- Stable callback ref (no stale closure)
- Clears on unmount
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/use-interval.ts`
