# argv-tokenizer

Tokenize a shell command string into argv array.

## Requirements
- tokenize(cmd) returns string[]
- Handles single and double quoted segments
- Handles backslash escapes
- Handles environment variable placeholders
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/argv-tokenizer.ts`
