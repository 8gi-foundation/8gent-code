# use-resize-observer

React hook wrapping ResizeObserver for element size tracking.

## Requirements
- useResizeObserver(ref) returns {width, height}
- Triggers on element size changes
- Debounced by default
- SSR-safe
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/use-resize-observer.ts`
