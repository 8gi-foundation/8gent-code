# transform-pipeline

Composable sync and async data transformation pipeline.

## Requirements
- pipe(...fns) creates a sync pipeline
- asyncPipe(...fns) creates an async pipeline
- Both accept initial value and return transformed result
- Type-safe chaining
- Zero dependencies

## Status

Quarantine - pending review.

## Location

`packages/tools/transform-pipeline.ts`
