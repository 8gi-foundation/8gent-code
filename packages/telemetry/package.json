{
	"name": "@8gent/telemetry",
	"version": "0.1.0",
	"type": "module",
	"main": "index.ts",
	"description": "Per-tenant attribution telemetry for LLM, vessel, and storage operations. Emits structured JSON for Vector + Loki. OpenTelemetry-compatible spans.",
	"scripts": {
		"test": "bun test"
	}
}
