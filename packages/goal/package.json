{
	"name": "@8gent/goal",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Goal-loop orchestrator (/go). Executor + judge + budget + receipt. Pure logic, no I/O beyond passed-in handles.",
	"main": "index.ts",
	"types": "index.ts",
	"exports": {
		".": {
			"import": "./index.ts",
			"types": "./index.ts"
		}
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"devDependencies": {
		"typescript": "^5.3.0"
	}
}
